pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
                script {
                    sh 'sudo npm install'
                }
            }
        }
    }
}
